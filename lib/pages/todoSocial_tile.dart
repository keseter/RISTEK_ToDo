import 'package:flutter/material.dart';

class SocialTile extends StatelessWidget {
  const SocialTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
